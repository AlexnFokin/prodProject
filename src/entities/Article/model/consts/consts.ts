export enum ArticleSortField {
  VIEWS = 'views',
  CREATED = 'createdAt',
  TITLE = 'title'
}

export enum ArticleBlockType {
  CODE = 'CODE',
  TEXT = 'TEXT',
  IMAGE = 'IMAGE'
}

export enum ArticleView {
  LIST = 'LIST',
  GRID = 'GRID'
}
